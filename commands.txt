    1  sudo apt update && sudo apt upgrade -y
        sudo raspi-config
    2  sudo reboot
    3  sudo apt install -y curl git build-essential
    4  curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
    5  sudo apt install -y nodejs
    6  node -v
    7  npm -v
    8  sudo npm install -g pm2
   10  sudo apt install nginx -y
   11  sudo systemctl enable nginx
   12  sudo systemctl start nginx
   13  systemctl status nginx
   15  ls
        sudo reboot
   17  sudo apt update
   20  git config --global user.name "osti-softdev"
   22  git config --global user.email "dev@osti.com.ph"
   23  git config --list
   25  git clone https://github.com/osti-softdev/OpenQSocketMultiple.git
   26  ls
   27  cd OpenQSocketMultiple
   28  ls
   30  git branch -r
   31  git checkout TOPLINE
   32  git branch -r
   33  git status
   34  git pull
   35  ls
   36  npm install
   40  rm -rf node_modules package-lock.json
   41  npm install
   43  sudo reboot
   44  ls
   63  python3 -m venv venv
   64  source venv/bin/activate

   69  pip install --upgrade pip setuptools wheel
   71  sudo apt install python3-venv python3-dev build-essential
   72  pip install --upgrade pip setuptools wheel
   73  npm install
   74  npm install onoff
   75  deactivate
   77  sudo npm install -g pm2
   78  npm start
        ctrl+c
   79  pm2 start index.js
   80  pm2 list
   81  pm2 save
   82  pm2 startup
    copy given command like below
   83  sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u osti --hp /home/osti
   84  pm2 list
   85  pm2 monit
   89  cd ..
   93  sudo systemctl is-enabled nginx
   94  sudo systemctl status nginx
   95  pm2 status index
   96  pm2 monit
   97  cd OpenQSocketMultiple
   98  ls
   99  cd outfolder
  102  ls
  103  cd config
  104  ls
  105  nano .env
  106  cd ..
  139  {- OPEN FILE NGINX_CONFIG.txt and follow all instructions - }
  270  pm2 monit
 194  source venv/bin/activate
  195  python -m pip install --upgrade pip
  196  python -m pip install gpiozero
 python -c "from gpiozero import Button, LED; print('GPIOZERO OK')"
204  npm start
  205  python -m pip install RPLCD
npm start
python -m pip install smbus2
npm start
sudo apt install -y swig build-essential python3-dev
  228  sudo apt update
sudo apt install -y liblgpio-dev~
  233  cat /etc/os-release
  234  apt-cache search lgpio
  235  sudo apt update
  236  sudo apt install -y liblgpio-dev liblgpio1
  237  dpkg -L liblgpio-dev | grep liblgpio
  238  ldconfig -p | grep lgpio
  239  python -m pip install --no-cache-dir lgpio
  240  python -c "import lgpio; print('LGPIO OK')"
  241  npm start

