# API Testing

This directory is reserved for API integration tests. These tests evaluate the endpoints of your Express application to ensure they return the correct responses, status codes, and data structures.

**Tools you can use here:**
- Supertest
- Postman / Newman
- Jest (with Supertest)

**Focus:** Route handlers, middleware, request validation, payload responses (e.g., `/api/tickets`, `/api/forward`).
